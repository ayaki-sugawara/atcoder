#include <bits/stdc++.h>
using namespace std;

int main() {
    int N;
    cin >> N;
    priority_queue<int, vector<int>, greater<int>> stack;
    for(int i=1; i<N; i++) {
        int a;
        cin >> a;
        if(a == i)
    }
    return 0;
}
